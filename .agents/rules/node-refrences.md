---
trigger: always_on
---

**Always create Export variables**: Always create export variables when refrencing nodes rather than hardcoding paths / searching the node tree. This does not apply to autoload classes. 